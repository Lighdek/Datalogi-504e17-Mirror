from . import *

# TODO: to be implemented; this is just a suggestion
# model = Model([
#   InputLayer(Image),
#   ConvolutionalLayer(shape=(3,3), stride=1, outFeatures=8),
#   PoolingLayer(type=max_pooling, shape=(2,2)/*stride and/or overlap?*/),
#   ActivationLayer(relu)
# ])
# model.add(InputLayer(Image))
# model.add(ConvolutionalLayer(shape=(3,3), stride=1, outFeatures=8))  # No need to specify layer dimensions
# model.add(PoolingLayer(type=max_pooling, shape=(2,2)/*stride and/or overlap?*/))
# model.add(ActivationLayer(relu))  # Not sure about order of layers
